'use client'; import {useEffect,useState} from 'react'; import {Clock3} from 'lucide-react';
export default function StatusBanner(){const [s,setS]=useState<any>(null);useEffect(()=>{fetch((process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000')+'/api/analytics/status').then(r=>r.json()).then(setS)},[]);return <div className="card p-4 flex items-center justify-between gap-4"><div className="flex items-center gap-3"><Clock3 size={19}/><div><div className="font-semibold">{s?.open?'Analysis window open':'Analysis window closed'}</div><div className="text-sm muted">{s?.message||'Loading IST status…'}</div></div></div><div className="text-sm font-medium">{s?.current_ist?new Date(s.current_ist).toLocaleTimeString('en-IN',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit'}):'—'} IST</div></div>}
